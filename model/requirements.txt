# GOaT model runtime — generated from model/pyproject.toml
# (base dependencies + ocr + mt + train extras; paddle pinned, see below).
# Single source for installs: `colab install -s goat -r requirements.txt`
# and selection.ipynb `%pip install -r requirements.txt`. Keep in sync
# with pyproject.toml when extras change.
numpy>=1.26,<2  # imgaug (via synthtiger) uses np.sctypes, removed in numpy 2
opencv-python-headless>=4.8
scipy>=1.13
jiwer>=3.0
sacrebleu>=2.4
tqdm>=4.66
psutil>=6.0
matplotlib>=3.11.1
paddleocr>=3.0
--extra-index-url https://www.paddlepaddle.org.cn/packages/stable/cu126/
paddlepaddle-gpu==3.2.*  # gpu wheel; 3.3.x breaks PaddleX det, PaddlePaddle/Paddle#77340
onnxruntime>=1.19
transformers>=4.46
torch>=2.5
ctranslate2>=4.4
sentencepiece>=0.2
datasets>=3.0
peft>=0.13
accelerate>=1.0
git+https://github.com/clovaai/synthtiger.git
